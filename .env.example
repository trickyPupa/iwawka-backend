# PostgreSQL
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# ClickHouse (для хранения логов)
CLICKHOUSE_DB=default
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
CLICKHOUSE_HTTP_PORT=8123
CLICKHOUSE_NATIVE_PORT=9000

# Application
APP_HOST=0.0.0.0
APP_PORT=8080

# JWT Authentication
AUTH_JWT_ISSUER=iwawka
AUTH_JWT_AUDIENCE=iwawka-audience
AUTH_JWT_SECRET=change-me-to-random-secret-in-production
AUTH_JWT_ACCESS_TTL_MINUTES=15
AUTH_JWT_REFRESH_TTL_DAYS=30

# Flyway
FLYWAY_ENABLED=true
FLYWAY_CLEAN_ON_ERROR=false

GIGACHAT_API_KEY=api_key